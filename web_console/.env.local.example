# Browser -> API on the host-published Docker port.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Next.js Server Components -> API.
# Use http://api:8000 when Next.js itself runs in docker-compose,
# or http://127.0.0.1:8000 when running `npm run dev` on the host.
API_URL=http://127.0.0.1:8000
